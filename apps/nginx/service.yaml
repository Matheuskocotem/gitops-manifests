apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: nginx-demo
spec:
  selector:
    app: nginx          # roteia tráfego pros pods com esse label
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP       # acessível só dentro do cluster (suficiente pro teste)
